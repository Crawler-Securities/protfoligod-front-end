import axios from 'axios';

export const fetchData = async () => {
  // const response = await axios.get('http://your-api-url.com/data-endpoint');
  // return response.data;
  return {
    data: 'Some data from the API'
  };
};